import '../../../export/exports.dart';

class JobDetailsDetailsTab extends StatelessWidget {
  const JobDetailsDetailsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Details Tab Content'));
  }
}